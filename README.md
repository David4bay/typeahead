# Autocomplete App

A small autocomplete dictionary application using nextjs

## Setup

- Clone the repo using `git clone <repo url>`
- Install dependencies `npm install`
- Build the project `npm run build`
- Run the build `npm start`
